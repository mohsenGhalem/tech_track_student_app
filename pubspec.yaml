name: tech_track_student_app
description: Tech Track Bootcamp Student app
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  #HTTP requests
  http: ^1.1.2
  # Date Formating
  intl: ^0.19.0
  #Local Data Storage
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
